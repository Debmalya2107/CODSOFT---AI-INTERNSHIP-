import java.util.*;
public class p16
{
	public static void main(String args[])
	{
		Scanner sc=new Scanner(System.in);
		System.out.println("Enter the value which u want to calculate: ");
		double a=sc.nextDouble();
		double square=a*a;
		double cube=a*a*a;
		System.out.println("so now the square value is=" +square);
		System.out.println(" so now the cube value is=" +cube);
	}
}